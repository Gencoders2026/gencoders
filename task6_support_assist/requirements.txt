# Task 6 - Support Assistance Agents
# (Coaching & Response Suggestion + Escalation Risk Monitor)
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1

# Tests (fastapi.testclient needs httpx)
pytest==9.1.1
httpx==0.27.2

# NOTE: the Knowledge Recommendation agent reuses the RAG retriever in
# `../rag`, whose dependencies (sentence-transformers, faiss-cpu, ...)
# are listed in `../rag/requirements.txt`. The bridge degrades
# gracefully when they are not installed.
